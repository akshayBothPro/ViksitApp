<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="3dip" />
            <gradient
                android:startColor="@color/progress_bar_bg_color"
                android:centerColor="@color/progress_bar_bg_color"
                android:endColor="@color/progress_bar_bg_color"
                />
        </shape>
    </item>

    <!--<item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="3dip" />
                <gradient
                    android:startColor="#80ffd300"
                    android:centerColor="#80ffb600"
                    android:centerY="0.75"
                    android:endColor="#a0ffcb00"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>-->

    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners
                    android:radius="5dip" />
                <gradient
                    android:startColor="@color/role_depth_red_progress"
                    android:endColor="@color/role_depth_red_progress"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>

</layer-list>